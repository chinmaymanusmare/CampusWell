const request = require('supertest');
const app = require('../src/app');

describe.skip('referral integration placeholders (skipped)', () => {
	test('placeholder - skipped', () => {
		expect(true).toBe(true);
	});
});