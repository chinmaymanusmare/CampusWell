<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title>Add Prescription</title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .form-container {max-width:600px;margin:30px auto;padding:28px;background:#f0f0f0;border:1px solid #ddd;border-radius:8px;}
    .form-title {text-align:center;font-size:1.6rem;margin-bottom:24px;}
    .form-group {display:flex;gap:12px;align-items:center;margin-bottom:14px;}
    .form-group label {flex:0 0 30%;font-weight:500;}
    .form-group input,.form-group textarea {flex:1;padding:10px;border:1px solid #aaa;border-radius:5px;background:#fff;font-family:inherit;}
    textarea {min-height:80px;resize:vertical;}
    .form-group input[readonly] {background:#e9ecef;color:#495057;}
    .actions {text-align:center;margin-top:20px;}
    .btn {background:#0d6efd;color:#fff;text-decoration:none;border:none;padding:12px 26px;border-radius:6px;font-weight:600;cursor:pointer;display:inline-block;}
    .btn-secondary {background:#0d6efd;margin-top:12px;}
  </style>
</head>
<body>
  <%- include('partials/header', { navHtml: '<a href="/logout" class="btn">LOG OUT</a>' }) %>
  <main class="main-content">
    <div class="form-container">
      <h2 class="form-title">Add Prescription</h2>
      <form action="/add-prescription" method="POST">
        <div class="form-group">
          <label for="student_id">Student ID</label>
          <input type="number" id="student_id" name="student_id" required />
        </div>
        <div class="form-group">
          <label for="date">Date</label>
          <input type="date" id="date" name="date" value="<%= new Date().toISOString().split('T')[0] %>" required />
        </div>
        <div class="form-group">
          <label for="doctor_name">Doctor</label>
          <input type="text" id="doctor_name" name="doctor_name" value="<%= username %>" readonly />
        </div>
        <div class="form-group">
          <label for="diagnosis">Diagnosis</label>
          <input type="text" id="diagnosis" name="diagnosis" required />
        </div>
        <div class="form-group">
          <label for="medicines">Medicines</label>
          <textarea id="medicines" name="medicines" required></textarea>
        </div>
        <div class="form-group">
          <label for="notes">Remarks</label>
          <textarea id="notes" name="notes"></textarea>
        </div>
        <div class="actions">
          <button type="submit" class="btn">Add</button><br />
          <a href="/users/<%= userid %>" class="btn btn-secondary">Back to Dashboard</a>
        </div>
      </form>
    </div>
  </main>
  <%- include('partials/footer') %>
</body>
</html>