<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Appointments</title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .container {
      max-width: 900px;
      margin: 30px auto;
      padding: 20px;
    }

    .title {
      text-align: center;
      font-size: 1.8rem;
      margin-bottom: 24px;
    }

    .card {
      background: #f0f0f0;
      padding: 18px;
      border: 1px solid #ddd;
      border-radius: 8px;
      margin-bottom: 16px;
    }

    .row {
      display: flex;
      margin-bottom: 10px;
    }

    .row strong {
      width: 150px;
      flex-shrink: 0;
    }

    .back {
      display: inline-block;
      margin-top: 20px;
    }
  </style>
</head>

<body>
  <%- include('../partials/header', { navHtml: '<a href="/logout" class="btn">LOG OUT</a>' }) %>
    <main class="main-content">
      <div class="container">
        <h1 class="title">Appointments</h1>
        <% if ((appointments||[]).length) { %>
          <% appointments.forEach(a=> { %>
            <div class="card">
              <div class="row"><strong>Student Id:</strong> <span>
                  <%= a.student_id %>
                </span></div>
              <div class="row"><strong>Student Name:</strong> <span>
                  <%= a.student_name %>
                </span></div>
              <div class="row"><strong>Date:</strong> <span>
                  <%= new Date(a.date).toLocaleDateString('en-GB') %>
                </span></div>
              <div class="row"><strong>Time:</strong> <span>
                  <%= a.time %>
                </span></div>
            </div>
            <% }) %>
              <% } else { %>
                <p>No scheduled appointments.</p>
                <% } %>
                  <a href="/users/<%= userid %>" class="btn back">Back to Dashboard</a>
      </div>
    </main>
    <%- include('../partials/footer') %>
</body>

</html>