<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Update Stock</title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .form-container {
      max-width: 500px;
      margin: 30px auto;
      padding: 28px;
      background: #fff;
    }

    .form-title {
      text-align: center;
      font-size: 1.6rem;
      margin-bottom: 24px;
    }

    .form-group {
      display: flex;
      gap: 12px;
      align-items: center;
      margin-bottom: 14px;
    }

    .form-group label {
      flex: 0 0 35%;
      font-weight: 500;
    }

    .form-group select,
    .form-group input {
      flex: 1;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      background: #f5f5f5;
    }

    .actions {
      text-align: center;
      margin-top: 24px;
    }

    .btn-secondary {
      background: #0d6efd;
      margin-top: 12px;
    }
  </style>
</head>

<body>
  <%- include('../partials/header') %>
    <main class="main-content">
      <div class="form-container">
        <h2 class="form-title">Update Stock</h2>
        <form action="/update-stock" method="POST">
          <div class="form-group">
            <label for="medicine_id">Medicine</label>
            <select id="medicine_id" name="medicine_id" required>
              <option value="" disabled selected>Select</option>
              <% (medicines||[]).forEach(m=> { %>
                <option value="<%= m.id %>">
                  <%= m.name %> (Stock: <%= m.stock %>)
                </option>
                <% }) %>
            </select>
          </div>
          <div class="form-group">
            <label for="quantity">Quantity to Add</label>
            <input type="number" id="quantity" name="quantity" placeholder="e.g. 50 or -10" required />
          </div>
          <div class="actions">
            <button type="submit" class="btn">Update</button><br />
            
          </div>
        </form>
      </div>
    </main>
    <%- include('../partials/footer') %>
</body>

</html>